cache:
  preload:
    module: myapp
    urls:
      - api/simple-list
      - api/long-list
    parameterizedUrls:
      - url: api/steps/{by}
        parameters:
          by: some expression

server:
  context-path: /test
