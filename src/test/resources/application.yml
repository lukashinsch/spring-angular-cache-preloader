cache:
  preload:
    module: myapp
    urls:
      - api/simple-list
      - api/long-list
    parametrizedUrls:
      - url: api/steps/{by}
        parameters:
          by: 2

server:
  context-path: /test
